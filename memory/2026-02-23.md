# 2026-02-23

## 자원 판매 메뉴 + 인벤토리 자원 시세 표시 완료

### 구현 내용
- `/sell_resources` 신규 커맨드 추가
  - 보유 자원 목록 + NPC 매입가 표시
  - 자원 선택 셀렉트 메뉴 + `판매하기` 버튼
  - 빠른 판매 버튼(`절반 판매`, `전체 판매`) 추가
  - 판매 수량 모달(`sellres:sellmodal:{itemKey}`) 추가
  - 반영 파일: `src/commands/sell_resources.js`, `src/bot.js`, `src/commands/help.js`
- NPC 자원 판매 로직 재사용 구조로 정리
  - `npc_shop` 판매 트랜잭션을 `executeNpcResourceSale` 함수로 추출
  - `/npc_shop` 모달 판매와 `/sell_resources` 빠른판매/모달판매가 동일 로직 사용
  - 반영 파일: `src/commands/npc_shop.js`
- `inventory.js` 자원 탭 확장
  - 새 탭 `자원` 추가 (`inventory:tab:resource`)
  - 자원별 NPC 현재 매입가 표시
  - `ItemPriceHistory`에서 1시간 전 가격 조회 후 등락률 계산
  - 추세 이모지(`📈/📉/➖`) 표시
  - 반영 파일: `src/commands/inventory.js`
- `ItemPriceHistory` 1시간 단위 자원 스냅샷 추가
  - `dynamic-pricing`에 자원 가격 스냅샷 생성 함수 추가
  - `economy-scheduler`에 1시간 주기 스냅샷 크론 추가
  - 반영 파일: `src/game/dynamic-pricing.js`, `src/game/economy-scheduler.js`

### 테스트/검증
- 신규 테스트 추가
  - `tests/commands/sell_resources.test.js`
  - `tests/game/economy-scheduler.test.js`
  - `tests/commands/inventory.test.js` 자원 탭 케이스 추가
  - `tests/game/dynamic-pricing.test.js` 스냅샷 row 변환 케이스 추가
- `npm test -- --runInBand` 통과
  - 결과: `20 suites, 71 tests` 전부 통과
- Discord 봇 실제 실행 스모크 시도: `node src/bot.js`
  - 결과: 네트워크 DNS 제한으로 `discord.com` 조회 실패 (`ENOTFOUND`)

---

## NPC 재료 매수 시스템 완성

### 구현 내용
- `/npc_shop` 상세 화면 판매 UX 확장
  - `💰 NPC에게 판매` 버튼 추가 (보유 수량 없으면 비활성화)
  - 판매 수량 입력 모달(`npcshop:sellmodal:{itemKey}`) 추가
  - 반영 파일: `src/commands/npc_shop.js`, `src/bot.js`
- NPC 재료 판매 거래 처리 구현
  - 재고 검증(`character.resources`) 후 수량 차감
  - 골드 지급 + `tradeVolume` 증가
  - `TradeHistory` 기록 (`sellerId=character.id`, `buyerId=0`)
  - `DynamicPrice.supply24h` 즉시 증가(upsert increment)
  - 반영 파일: `src/commands/npc_shop.js`
- NPC 상점 소모품 판매 추적 + 수요 반영
  - Prisma `NpcShopSale` 모델 추가
  - `shop.js` 포션 구매 시 `NpcShopSale` 기록
  - `dynamic-pricing`에서 최근 24h `NpcShopSale`을 레시피 재료 수요로 환산해 `demand24h`에 반영
  - 경제 스케줄러에 동적 가격 주기 갱신 크론 추가
  - 반영 파일: `src/database/schema.prisma`, `prisma/schema.prisma`, `src/commands/shop.js`, `src/game/dynamic-pricing.js`, `src/game/economy-scheduler.js`, `.env.example`

### 테스트/검증
- `npm run prisma:generate` 통과
- `npm test` 통과 (18 suites, 65 tests)
- 실제 봇 실행 스모크 시도: `node src/bot.js`
  - 결과: 네트워크 DNS 제한으로 `discord.com` 조회 실패 (`ENOTFOUND`)

---

## 경제 시스템 확장 Part 2 완료

### 구현 내용
- `FeeConfig`, `ItemPriceHistory` 모델 추가
  - 반영 파일: `src/database/schema.prisma`, `prisma/schema.prisma`
- 동적 수수료 시스템 구현
  - 신규 모듈: `src/game/fee-config.js`
  - 24h 거래량 기반 자동 조정(저활성/중간/고활성 구간)
  - 수동 조정 시 자동 모드 해제, 자동 ON/OFF 토글 지원
- 거래소/젬 수수료를 동적 요율로 전환
  - `src/commands/market.js`
  - `src/commands/gem.js`
  - `src/game/gem-exchange.js`
- 경제 모니터 확장
  - `ItemPriceHistory` 저장 로직 추가
  - 7일 가격 추이 집계/스파크라인 차트 데이터 생성
  - 아이템 기준 거래량 TOP 집계 추가
  - 반영 파일: `src/game/economy-monitor.js`
- `/economy_admin` 확장 (버튼 UI)
  - 대시보드/통계/알림/스냅샷/수수료 화면 전환 버튼
  - 수수료 새로고침/자동 토글/증감 버튼
  - 반영 파일: `src/commands/economy_admin.js`, `src/bot.js`
- `/stats` 신규 커맨드 추가
  - 거래량 TOP 10
  - 가격 추이 차트(7일)
  - 전체/자원/장비 필터 버튼 + 새로고침 버튼
  - 반영 파일: `src/commands/stats.js`, `src/bot.js`, `src/commands/help.js`

### 검증
- `npm test` 통과 (17 suites, 59 tests)
- `npm run prisma:generate` 통과
- `node --check`로 주요 변경 파일 문법 점검 완료

### 참고
- Discord 실제 서버 상호작용 테스트는 토큰/서버 환경에서 추가 확인 필요

---

## 경제 시스템 확장 Part 1 완료

### 구현 내용
- Prisma 모델 추가
  - `DynamicPrice`, `Auction`, `AuctionBid`
  - 반영 파일: `src/database/schema.prisma`, `prisma/schema.prisma`
  - `Character` 모델에 경매 관계 필드(`auctionsAsSeller`, `auctionsAsHighestBidder`, `auctionBids`) 추가
- NPC 동적 가격 시스템
  - 신규 모듈: `src/game/dynamic-pricing.js`
  - 24시간 수급/거래 기반 동적 멀티플라이어 및 NPC 매입/판매가 계산
  - `DynamicPrice` upsert/조회/전체 갱신 로직 구현
- `/npc_shop` 신규 커맨드
  - 반영 파일: `src/commands/npc_shop.js`
  - 동적 가격 목록/상세 조회
  - 버튼 + 셀렉트 UI (`시세 갱신`, `전체 목록`, 자원 선택)
- `/auction` 신규 커맨드
  - 반영 파일: `src/commands/auction.js`
  - 경매장 메인/내 경매/상세 화면
  - 경매 등록(희귀 이상, 미장착 장비), 입찰, 판매자 수동 종료 버튼
  - 마감 임박 입찰 자동 연장(5분)
- 경매 정산 도메인 + 자동 종료 크론
  - 신규 모듈: `src/game/auction-house.js`, `src/game/auction-scheduler.js`
  - 낙찰/유찰 정산(아이템 이전·반환, 수수료 정산, 기존 최고입찰 환불)
  - 봇 시작/종료 시 경매 정산 job 시작/정지 (`src/bot.js`)
- 런타임 라우팅/메뉴 연결
  - `src/bot.js`: `npc_shop` 버튼/셀렉트, `auction` 버튼/셀렉트/모달 라우팅 추가
  - `src/commands/village.js`: 마을 거래소 메뉴에 `NPC 상점`, `경매장` 진입 버튼 추가
  - `src/commands/help.js`: 경제 카테고리에 `npc_shop`, `auction` 추가

### 검증
- `npm test -- --runInBand` 통과 (17 suites, 59 tests)
- `npm run prisma:generate` 통과
- `npm run check` 통과

### 참고
- Discord 실제 서버 상호작용 테스트는 토큰/서버 환경에서 추가 확인 필요
