{
  "name": "homeland-discord-bot",
  "version": "1.0.0",
  "description": "HOMELAND Discord bot - Week 1 core combat system",
  "main": "src/bot.js",
  "scripts": {
    "build": "prisma generate --schema src/database/schema.prisma",
    "start": "prisma db push --schema src/database/schema.prisma --accept-data-loss --skip-generate && node src/bot.js",
    "dev": "node --watch src/bot.js",
    "prisma:generate": "prisma generate --schema src/database/schema.prisma",
    "prisma:migrate": "prisma migrate dev --name init --schema src/database/schema.prisma",
    "check": "node --check src/bot.js src/database/client.js src/commands/create.js src/commands/profile.js src/commands/explore.js src/game/monsters.js src/game/leveling.js src/game/skills.js src/game/combat.js",
    "lint": "eslint src --ext .js",
    "lint:fix": "eslint src --ext .js --fix",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "validate": "npm run lint && npm run check",
    "precommit": "npm run validate"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-mock-extended": "^4.0.0",
    "prisma": "^6.6.0"
  }
}
