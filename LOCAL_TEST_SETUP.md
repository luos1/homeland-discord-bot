# 🧪 로컬 테스트 환경 설정

**목적**: 프로덕션 배포 전에 로컬에서 완벽하게 테스트하기

---

## 📋 사전 준비

### 1. 환경 변수 설정

`.env.local` 파일 생성:
```bash
cd ~/homeland-discord-bot
cp .env .env.local
```

`.env.local` 편집:
```env
# Discord 봇 토큰 (테스트용 봇 권장)
DISCORD_TOKEN=your_test_bot_token

# 데이터베이스 (로컬 SQLite)
DATABASE_URL="file:./dev.db"

# 로그 채널 (테스트 서버)
ERROR_LOG_CHANNEL_ID=your_test_channel_id

# 기타 필요한 변수들...
```

### 2. 테스트용 Discord 봇 생성

**왜 필요?**: 프로덕션 봇과 분리해서 테스트

**방법**:
1. https://discord.com/developers/applications 접속
2. "New Application" 클릭
3. 이름: "HOMELAND-TEST" (또는 원하는 이름)
4. Bot 탭 → "Add Bot"
5. Token 복사 → `.env.local`에 추가
6. 테스트 Discord 서버에 초대

---

## 🚀 로컬 서버 실행

### 1. 데이터베이스 마이그레이션
```bash
cd ~/homeland-discord-bot
npx prisma migrate dev
npx prisma generate
```

### 2. 서버 시작
```bash
npm run dev
```

**확인사항**:
- [ ] "Ready!" 메시지 출력
- [ ] 에러 없음
- [ ] Discord에서 봇이 온라인 상태

---

## 🧪 테스트 프로세스

### 1. 자동 테스트 실행
```bash
./test-before-deploy.sh
```

**결과**:
- ✅ 모든 테스트 통과 → 다음 단계
- ❌ 테스트 실패 → 수정 후 재테스트

### 2. 수동 테스트 (Discord에서)

#### A. 기본 명령어
```
/play
/profile
/shop
```

#### B. 전투 시스템 (핵심!)
```
1. /play 실행
2. "탐험" 버튼 클릭
3. 사냥터 선택 (예: 어둠의 동굴)
4. 전투 시작 확인
5. 모든 버튼 테스트:
   - 공격
   - 방어
   - 포션
   - 스킬
   - 도망
6. 전투 완료 확인
```

#### C. 새 기능 (추가한 경우)
- [ ] 새 명령어 실행
- [ ] 모든 버튼 클릭
- [ ] 에러 케이스 확인

### 3. 여러 시나리오 테스트

**최소 3개 사냥터**:
- [ ] 어둠의 동굴
- [ ] 죽음의 산맥
- [ ] 지하 던전

**다양한 상황**:
- [ ] 승리
- [ ] 패배
- [ ] 도망
- [ ] 포션 사용
- [ ] HP 0 상태

---

## 📸 테스트 결과 기록

### 스크린샷 찍기
1. 전투 시작 화면
2. 각 버튼 클릭 결과
3. 전투 완료 화면
4. 에러 발생 시 에러 메시지

### 로그 확인
```bash
# 콘솔 로그 확인
tail -f logs/app.log  # 있는 경우

# 또는 터미널 출력 확인
```

---

## ✅ 배포 가능 기준

**모두 충족해야 배포 가능**:

1. [ ] `./test-before-deploy.sh` 통과
2. [ ] 로컬에서 전투 시스템 정상 작동
3. [ ] 3개 이상 사냥터에서 테스트 완료
4. [ ] 모든 버튼 작동 확인
5. [ ] 에러 없음
6. [ ] 테스트 결과 스크린샷 준비
7. [ ] `TEST_CHECKLIST.md` 작성 완료
8. [ ] 형아 승인 받음

---

## 🚫 배포 금지 조건

**하나라도 해당되면 배포 금지**:

- ❌ 로컬 테스트 안 함
- ❌ 자동 테스트 실패
- ❌ 전투 버튼 에러 발생
- ❌ 순환 참조 존재
- ❌ 형아 승인 없음

---

## 🎯 배포 프로세스

**테스트 완료 후**:

1. 테스트 결과 공유
```
형아! 로컬 테스트 완료했습니다.

[스크린샷 1: 전투 시작]
[스크린샷 2: 공격 버튼]
[스크린샷 3: 전투 완료]

자동 테스트: ✅ 통과
수동 테스트: ✅ 3개 사냥터
에러: 없음

배포해도 될까요?
```

2. 형아 승인 대기

3. 승인 받으면 배포
```bash
git push origin main
```

4. Railway 배포 확인 (2-3분)

5. 프로덕션에서 빠른 테스트

---

## 💡 팁

### 빠른 테스트
```bash
# 자동 테스트 → 로컬 실행 → Discord 테스트 (한 번에)
./test-before-deploy.sh && npm run dev
```

### 에러 발생 시
1. 즉시 로컬 서버 중지
2. 에러 로그 확인
3. 문제 수정
4. 테스트 처음부터 다시

### 형아 부재 시
- 테스트 완료해두고 대기
- 급하게 배포 금지
- 승인 받을 때까지 로컬에서만 작업

---

## 🎯 최종 원칙

**테스트 없이 배포하는 것 = 서비스 중단 각오**

**로컬에서 100% 작동하는 것만 프로덕션에 배포**

**형아 승인 = 필수**
